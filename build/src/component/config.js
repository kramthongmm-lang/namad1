    // ดึง hostname จาก URL ที่ผู้ใช้กำลังเปิดอยู่
    const HOSTNAME = window.location.hostname;

    // ตั้งค่า API base URL อัตโนมัติ
    export const API_BASE = `http://${window.location.hostname}:5000`;


